---

- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install Python runtime and build dependencies
  ansible.builtin.apt:
    name: "{{ runtime_apt_packages }}"
    state: present
